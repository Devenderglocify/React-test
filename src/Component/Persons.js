import React from 'react'

function Persons(per) {
  return (
    <div>
        <h3>
            Hi my name {per.name} and my age is {per.age} and my skill are {per.skill}
        </h3>
    </div>
  )
}

export default Persons